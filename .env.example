# TinyVecDB Configuration

# Embedding Model
# Options: Any HuggingFace model ID compatible with SentenceTransformers

# Default: TaylorAI/bge-micro-v2 (384-dim, tiny, very fast)
# Alternative: Snowflake/snowflake-arctic-embed-xs (384-dim, best overall, fast)

# Note: All models converted to ONNX format for performance.

EMBEDDING_MODEL=TaylorAI/bge-micro-v2
EMBEDDING_CACHE_DIR=~/.cache/tinyvecdb  # Model cache directory

# Batch size for embedding inference (optional - auto-detected if not set)
# Auto-detection considers: GPU VRAM, Apple Silicon, CPU cores, architecture
# Override only if you need specific batch size for your use case
# EMBEDDING_BATCH_SIZE=256

# Database
DATABASE_PATH=./data/tinyvecdb.db

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=53287
